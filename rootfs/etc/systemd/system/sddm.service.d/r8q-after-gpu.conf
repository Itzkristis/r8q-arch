# r8q: renderD128 (Adreno 650) only exists after r8q-gpu.service has modprobed msm.
# KWin picks its render device at startup; if sddm wins the race, KWin pairs simpledrm
# with the software renderer and the whole session runs on llvmpipe.
[Unit]
Wants=r8q-gpu.service
After=r8q-gpu.service
